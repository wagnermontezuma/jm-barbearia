{
    "version": 2,
    "framework": "vite",
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "dist",
    "functions": {
        "api/index.ts": {
            "runtime": "@vercel/node@5.1.0"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index"
        }
    ]
}